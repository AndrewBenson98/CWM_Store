spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store?createDatabaseIfNotExist=true
    username: root
    password: Rootpassword321

stripe:
  apiUrl: "https://api.stripe.com"
  enabled: true
  timeout: 1000
  supported-currencies: USD,EUR,GBP

payment:
  gateway: "paypal"

email:
  port: 3030
  host: "https://emailhost.com"

