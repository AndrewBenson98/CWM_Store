spring:
  application:
    name: store

stripe:
  apiUrl: "https://api.stripe.com"
  enabled: true
  timeout: 1000
  supported-currencies: USD,EUR,GBP

payment:
  gateway: "paypal"

email:
  port: 3030
  host: "https://emailhost.com"